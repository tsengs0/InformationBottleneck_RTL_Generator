./main 1 3

----------------------------------
@ T0
INIT
IDLE (HW_0) --> COL_ADDR_ARRIVAL (HW_0)
IDLE (HW_1) --> IDLE (HW_1)
IDLE (HW_2) --> IDLE (HW_2)

----------------------------------
@ T1
INIT
COL_ADDR_ARRIVAL (HW_0) --> READ_COL_ADDR (HW_0)
IDLE (HW_1) --> IDLE (HW_1)
IDLE (HW_2) --> IDLE (HW_2)

----------------------------------
@ T2
INIT
READ_COL_ADDR (HW_0) --> SHIFT_GEN (HW_0)
IDLE (HW_1) --> COL_ADDR_ARRIVAL (HW_1)
IDLE (HW_2) --> IDLE (HW_2)

----------------------------------
@ T3
SHIFT_GEN (HW_0) --> SHIFT_OUT (HW_0)
COL_ADDR_ARRIVAL (HW_1) --> READ_COL_ADDR (HW_1)
IDLE (HW_2) --> IDLE (HW_2)

----------------------------------
@ T4
SHIFT_OUT (HW_0) --> IDLE (HW_0)
READ_COL_ADDR (HW_1) --> SHIFT_GEN (HW_1)
IDLE (HW_2) --> COL_ADDR_ARRIVAL (HW_2)

----------------------------------
@ T5
IDLE (HW_0) --> IDLE (HW_0)
SHIFT_GEN (HW_1) --> SHIFT_OUT (HW_1)
COL_ADDR_ARRIVAL (HW_2) --> READ_COL_ADDR (HW_2)

----------------------------------
@ T6
IDLE (HW_0) --> COL_ADDR_ARRIVAL (HW_0)
SHIFT_OUT (HW_1) --> IDLE (HW_1)
READ_COL_ADDR (HW_2) --> SHIFT_GEN (HW_2)

----------------------------------
@ T7
COL_ADDR_ARRIVAL (HW_0) --> READ_COL_ADDR (HW_0)
IDLE (HW_1) --> IDLE (HW_1)
SHIFT_GEN (HW_2) --> SHIFT_OUT (HW_2)

----------------------------------
@ T8
READ_COL_ADDR (HW_0) --> SHIFT_GEN (HW_0)
IDLE (HW_1) --> COL_ADDR_ARRIVAL (HW_1)
SHIFT_OUT (HW_2) --> IDLE (HW_2)

----------------------------------
@ T9
SHIFT_GEN (HW_0) --> SHIFT_OUT (HW_0)
COL_ADDR_ARRIVAL (HW_1) --> READ_COL_ADDR (HW_1)
IDLE (HW_2) --> IDLE (HW_2)

----------------------------------
@ T10
SHIFT_OUT (HW_0) --> IDLE (HW_0)
READ_COL_ADDR (HW_1) --> SHIFT_GEN (HW_1)
IDLE (HW_2) --> COL_ADDR_ARRIVAL (HW_2)

----------------------------------
@ T11
IDLE (HW_0) --> IDLE (HW_0)
SHIFT_GEN (HW_1) --> SHIFT_OUT (HW_1)
COL_ADDR_ARRIVAL (HW_2) --> READ_COL_ADDR (HW_2)

----------------------------------
@ T12
IDLE (HW_0) --> COL_ADDR_ARRIVAL (HW_0)
SHIFT_OUT (HW_1) --> IDLE (HW_1)
READ_COL_ADDR (HW_2) --> SHIFT_GEN (HW_2)

----------------------------------
@ T13
COL_ADDR_ARRIVAL (HW_0) --> READ_COL_ADDR (HW_0)
IDLE (HW_1) --> IDLE (HW_1)
SHIFT_GEN (HW_2) --> SHIFT_OUT (HW_2)

----------------------------------
@ T14
READ_COL_ADDR (HW_0) --> SHIFT_GEN (HW_0)
IDLE (HW_1) --> COL_ADDR_ARRIVAL (HW_1)
SHIFT_OUT (HW_2) --> IDLE (HW_2)

----------------------------------
@ T15
SHIFT_GEN (HW_0) --> SHIFT_OUT (HW_0)
COL_ADDR_ARRIVAL (HW_1) --> READ_COL_ADDR (HW_1)
IDLE (HW_2) --> IDLE (HW_2)

----------------------------------
@ T16
SHIFT_OUT (HW_0) --> IDLE (HW_0)
READ_COL_ADDR (HW_1) --> SHIFT_GEN (HW_1)
IDLE (HW_2) --> COL_ADDR_ARRIVAL (HW_2)

----------------------------------
@ T17
IDLE (HW_0) --> IDLE (HW_0)
SHIFT_GEN (HW_1) --> SHIFT_OUT (HW_1)
COL_ADDR_ARRIVAL (HW_2) --> READ_COL_ADDR (HW_2)

----------------------------------
@ T18
IDLE (HW_0) --> COL_ADDR_ARRIVAL (HW_0)
SHIFT_OUT (HW_1) --> IDLE (HW_1)
READ_COL_ADDR (HW_2) --> SHIFT_GEN (HW_2)

----------------------------------
@ T19
COL_ADDR_ARRIVAL (HW_0) --> READ_COL_ADDR (HW_0)
IDLE (HW_1) --> IDLE (HW_1)
SHIFT_GEN (HW_2) --> SHIFT_OUT (HW_2)
